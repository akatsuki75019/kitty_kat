<!--Jumbotron / hero section-->
<section class="">
  <%= render 'hero' %>
</section>

<section class="">
  <%= render 'categories' %>
</section>

<!--Voir si on ajoute la section static sur les bienfaits du site-->


<!--Section Index Items avec les 10 derniers items-->
<div class="bg-white py-12">
  <div class="isolate bg-white px-6 lg:px-8">
    <div class="mx-auto max-w-2xl text-center">
      <div class="text-center text-neutral-600 text-base font-semibold leading-relaxed">DERNIERS AJOUTS</div>
      <h2 class="font-medium tracking-tight text-neutral-900 text-2xl md:text-5xl">Pour toujours plus de choix, notre stock est complété quotidiennement.</h2>
      <p class="mt-2 mb-10 text-lg leading-8 text-gray-600">Nos tout derniers minous 😻😻😻</p>
    </div>
    <div class="container mx-auto grid max-w-2xl grid-cols-1 gap-x-1 gap-y-1 pt-3 mt-3 lg:mt-2 lg:pt-2 lg:mx-0 lg:max-w-none md::grid-cols-2 lg:grid-cols-3">
      <% @latest_items = @items.last(9) %>
      <% @latest_items.each do |item| %> 
        <article class="flex max-w-xl flex-col items-center justify-between">
          <div class="group relative">
            <div class="md:w-96 md:h-52 mb-1 justify-center overflow-hidden flex items-center inline-flex">
              <%= link_to(image_tag("#{item.image_url}", class: "w-full h-full object-cover", alt: "cat picture item here to sell"), item_path(item.id)) %>
            </div>
          </div>
          <div class="relative flex items-center gap-x-4 mb-10">
            <div class="text-sm leading-6">
              <p class="font-semibold text-gray-900">
                <%= link_to(item.title, item_path(item.id)) %>
              </p>
              <p class="text-gray-600 text-center"><%= item.price %> euros</p>
            </div>
          </div>
        </article>
      <% end %>
    </div>
  </div>
</div>


